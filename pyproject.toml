[project]
name = "rethinking-stats-and-pymc"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "marimo[recomended]>=0.19.4",
]
